<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.BelaJR" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/primary</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:statusBarColor">@color/primary_dark</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Background Splash Screen -->
        <item name="windowSplashScreenBackground">@android:color/white</item>

        <!-- Hilangkan ikon bulat standar Android 12 -->
        <item name="windowSplashScreenAnimatedIcon">@android:color/transparent</item>

        <!-- Paksa gunakan background kustom yang berisi gambar asli -->
        <item name="android:windowBackground">@drawable/splash_bg</item>

        <item name="postSplashScreenTheme">@style/Theme.BelaJR</item>
    </style>

    <style name="CircleImageView" parent="">
        <item name="cornerSize">50%</item>
    </style>

    <style name="RoundedSquareImageView" parent="">
        <item name="cornerSize">12dp</item>
    </style>
</resources>
